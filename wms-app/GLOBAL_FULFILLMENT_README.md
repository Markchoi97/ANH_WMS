# 🌐 ANH WMS - 해외배송 (Global Fulfillment) 시스템

중국 등 해외 고객사의 제품이 한국 창고에 입고되어 출고/통관/배송까지 관리되는 전체 프로세스를 하나의 메뉴 트리 안에서 단계별로 관리 및 추적 가능하게 구현한 시스템입니다.

## 📋 목차

1. [시스템 개요](#시스템-개요)
2. [데이터베이스 스키마](#데이터베이스-스키마)
3. [주요 기능](#주요-기능)
4. [페이지 구조](#페이지-구조)
5. [API 엔드포인트](#api-엔드포인트)
6. [다국어 지원](#다국어-지원)
7. [설치 및 사용](#설치-및-사용)

---

## 시스템 개요

### 목적
해외 플랫폼(Taobao, Shopify, Shopee 등)에서 주문된 상품의 국내 창고 입고부터 검수, 포장, 출고, 통관, 배송까지 전체 물류 프로세스를 체계적으로 관리합니다.

### 주요 특징
- ✅ **단계별 프로세스 관리**: 10개 단계로 세분화된 물류 워크플로우
- ✅ **실시간 추적**: 각 주문의 현재 상태를 실시간으로 확인
- ✅ **이상 처리**: 시스템 자동 감지 및 알림
- ✅ **다국어 지원**: 한글, 중문, 영문 3개 언어 지원
- ✅ **통관 관리**: 통관 상태 추적 및 지연 알림
- ✅ **운송사 연동**: CJ대한통운, 顺丰速运, Lotte Global 등

---

## 데이터베이스 스키마

### 주요 테이블

#### 1. `global_customers` (고객사)
해외 고객사 정보를 관리합니다.
```sql
- id (UUID)
- name (고객사명)
- country_code (국가 코드: CN, JP, KR 등)
- platform (플랫폼: Taobao, Shopify 등)
- wechat_id (WeChat ID)
- email, phone
- notification_preference (알림 설정)
```

#### 2. `global_fulfillment_orders` (주문)
해외배송 주문의 메인 테이블입니다.
```sql
- id (UUID)
- order_number (주문번호)
- customer_id (고객사 ID)
- platform_order_id (외부 플랫폼 주문번호)
- current_step (현재 단계)
- status (상태: pending, in_progress, completed 등)
- origin_country (출발 국가)
- destination_country (목적지 국가)
- shipping_method (운송 방식: air, sea, express)
- carrier (운송사)
- customs_status (통관 상태)
- total_weight, total_volume
- 각종 타임스탬프 (received_at, packed_at, shipped_at 등)
```

#### 3. `global_order_items` (주문 상세)
주문의 SKU별 상세 정보입니다.
```sql
- order_id (주문 ID)
- sku, product_name
- quantity (주문 수량)
- received_quantity (입고 수량)
- inspected_quantity (검수 수량)
- damaged_quantity (파손 수량)
- missing_quantity (누락 수량)
```

#### 4. `global_waves` (파도 관리)
출고 일정별 묶음 처리를 위한 Wave 테이블입니다.
```sql
- wave_number (Wave 번호)
- wave_type (standard, 2B, 2S, pallet)
- shipping_method, carrier
- status (planned, in_progress, completed)
- planned_ship_date (출고 예정일)
- cutoff_time (마감 시간)
```

#### 5. `global_inspections` (검수 기록)
제품 검수 결과 및 사진을 저장합니다.
```sql
- order_id, item_id
- inspection_type (quality, quantity, packaging)
- result (pass, fail, partial)
- condition (normal, damaged, missing)
- photos (검수 사진 배열)
- action_taken (approve, quarantine, return, dispose)
```

#### 6. `global_exceptions` (이상 처리)
전체 프로세스 중 발생한 오류/이상을 관리합니다.
```sql
- exception_number (이상번호)
- exception_type (missing_item, damaged, customs_delay 등)
- severity (low, medium, high, critical)
- status (open, investigating, resolved, escalated)
- detected_by (system, operator, customer)
- customer_notified (고객 알림 여부)
```

#### 7. `global_packages` (패키지 정보)
포장된 패키지의 정보를 관리합니다.
```sql
- package_number
- package_type (box, pallet, 2B, 2S)
- weight, length, width, height
- weight_verified (중량 검증 여부)
- tracking_label (송장 라벨)
```

#### 8. `global_returns` (교환/반품)
반품 및 교환 요청을 관리합니다.
```sql
- return_number
- return_type (return, exchange)
- reason (반품 사유)
- status (requested, approved, received, refunded)
- return_to_inventory (재고 복귀 여부)
```

#### 9. `global_cutoffs` (마감 시간)
운송사별 마감 시간을 관리합니다.
```sql
- cutoff_name
- carrier (운송사)
- cutoff_time (마감 시각)
- reminder_minutes_before (알림 시간)
```

### 데이터베이스 설치

```bash
# Supabase에서 스키마 실행
psql -h [YOUR_SUPABASE_HOST] -U postgres -d postgres < supabase-global-fulfillment-schema.sql

# 샘플 데이터 삽입
psql -h [YOUR_SUPABASE_HOST] -U postgres -d postgres < supabase-global-fulfillment-sample-data.sql
```

---

## 주요 기능

### 📊 메인 대시보드 (`/global-fulfillment`)
- 전체 주문 현황 요약 (입고/검수/패킹/출고/통관/이상)
- 프로세스 단계별 진행률 시각화
- 국가별/고객사별 물류량 차트
- 통관 지연 및 오류 TOP 5 표시
- 실시간 알림 및 통관 상태

### 🚚 드롭시핑 (`/global-fulfillment/drop-shipping`)
**목적**: 해외 플랫폼 주문의 국내 창고 입고 관리

**기능**:
- Excel/CSV 주문 데이터 업로드
- 플랫폼별 주문번호 자동 매핑 (Taobao, Shopify, Shopee)
- SKU별 재고 자동 차감
- 자동 송장번호 부여
- 바코드/QR 스캔 입고
- 템플릿 다운로드 기능

**Excel 양식**:
| 주문번호 | 고객사 | SKU | 상품명 | 수량 | 목적국가 | 배송방법 | 비고 |
|---------|--------|-----|--------|------|----------|----------|------|

### 📦 상품 준비 및 환적 (`/global-fulfillment/preparation`)
**목적**: 입고된 상품의 상태 확인 및 재포장, 국내외 배송 전처리

**기능**:
- 제품 상태 확인 (정상/불량/분실)
- 라벨 재부착, 세트 조립
- 환적지 선택 (한국 → 일본, 한국 → 중국 내)
- 출고 대기 목록 표시
- 출고 전 고객 승인 요청

### 🌊 파도 관리 (`/global-fulfillment/wave-management`)
**목적**: 출고 일정 및 분류 계획 자동화

**기능**:
- 출고 대상 SKU 자동 분류 (2B/2S 단위)
- 운송채널별 묶음 처리 (항공/해운)
- 출고 Batch 번호 자동 생성
- 분류 상태 모니터링 (완료/진행/지연/대기)

**Wave 타입**:
- **Standard**: 일반 출고 (혼합 배송)
- **2B**: 2개 박스 단위 묶음
- **2S**: 2개 세트 단위 묶음
- **Pallet**: 팔레트 단위 대량 출고

### 🔄 2차 정렬 (`/global-fulfillment/second-sorting`)
**목적**: 첫 분류 후, SKU별 세부 분류 및 수취인별 패키지 구성

**기능**:
- 배송번호 스캔 기반 자동 매칭
- 중복상품, 누락상품 검출
- 운송사별 파렛트(Pallet) 구성

### 🔍 검증/검사 (`/global-fulfillment/inspection`)
**목적**: 출고 전 제품 이상 여부 확인

**기능**:
- 이미지 캡처 및 이상품 업로드
- 검수 결과 상태 (정상/파손/분실)
- 자동 재고 반영 (불량품 → 격리재고 이동)
- 검수 사진 고객 공유 기능

**검수 프로세스**:
1. 검사 대기 목록에서 항목 선택
2. 검수 사진 촬영/업로드
3. 검사 결과 입력 (정상/일부/불량)
4. 자동으로 다음 단계로 이동

### 📋 패키지 검증 (`/global-fulfillment/package-check`)
**목적**: 실제 포장 단위 확인 및 송장 부착

**기능**:
- 포장 후 무게 자동 기록
- 송장 인쇄/라벨 부착
- 포장 완료 스캔 시 출고 준비 완료 상태로 전환

### ⚖️ 무게 측정 (`/global-fulfillment/weight-check`)
**목적**: 출고 요금 산정 및 중량 검증

**기능**:
- 바코드 스캔 → 무게값 자동 기록
- 중량 오류 자동 감지 (허용 오차 ±5%)
- 운임 산출 API 연동 (CJ, 로젠, 顺丰 등)

### 🔄 교환/반품 (`/global-fulfillment/returns`)
**목적**: 반품 또는 교환 요청건 처리

**기능**:
- 송장번호/주문번호로 조회
- 반품 사유 입력 및 사진 첨부
- 자동 재고 복귀 or 폐기 처리 옵션

### ⚠️ 비정상적인 부분 (`/global-fulfillment/exceptions`)
**목적**: 전체 프로세스 중 오류/누락건 집중 관리

**기능**:
- 시스템 자동 탐지 (누락, 중복, 통관 지연)
- 이상처리 보고서 자동 생성
- 고객 알림 전송 (WeChat/이메일)
- Severity 기반 우선순위 처리

**이상 유형**:
- `missing_item`: 상품 누락
- `duplicate`: 중복 주문
- `damaged`: 상품 파손
- `customs_delay`: 통관 지연 ⚠️
- `wrong_address`: 주소 오류
- `weight_mismatch`: 중량 불일치
- `system_error`: 시스템 오류

### ⏰ 마감 시간 (`/global-fulfillment/cutoff`)
**目的**: 매일/매주 단위 작업 마감 관리 (发货截止时间)

**기능**:
- 출고/입고 마감 시간 설정
- 당일 미처리건 자동 리마인드
- 운송사별 마감 시각 안내
  - 顺丰速运: 17:00
  - CJ대한통운: 18:00
  - Lotte Global: 16:00

---

## 페이지 구조

```
/app/global-fulfillment/
├── page.tsx                    # 메인 대시보드
├── drop-shipping/
│   └── page.tsx               # 드롭시핑 (주문 입고)
├── preparation/
│   └── page.tsx               # 상품 준비 및 환적
├── wave-management/
│   └── page.tsx               # 파도 관리
├── second-sorting/
│   └── page.tsx               # 2차 정렬
├── inspection/
│   └── page.tsx               # 검증/검사
├── package-check/
│   └── page.tsx               # 패키지 검증
├── weight-check/
│   └── page.tsx               # 무게 측정
├── returns/
│   └── page.tsx               # 교환/반품
├── exceptions/
│   └── page.tsx               # 이상 처리
└── cutoff/
    └── page.tsx               # 마감 시간
```

---

## API 엔드포인트

### 통계 API
```typescript
GET /api/global-fulfillment/stats
```
**응답**:
```json
{
  "totalOrders": 156,
  "pendingOrders": 12,
  "inProgressOrders": 45,
  "completedOrders": 89,
  "delayedOrders": 8,
  "exceptionOrders": 2,
  "byStep": {
    "drop_shipping": 12,
    "preparation": 15,
    ...
  },
  "byCountry": {
    "CN": 120,
    "JP": 25,
    "KR": 8
  },
  "topExceptions": [...]
}
```

### 주문 API
```typescript
// 주문 목록 조회
GET /api/global-fulfillment/orders?status=pending&step=inspection

// 새 주문 생성
POST /api/global-fulfillment/orders
```

### 이상 API
```typescript
// 이상 목록 조회
GET /api/global-fulfillment/exceptions?status=open&severity=high

// 새 이상 생성
POST /api/global-fulfillment/exceptions
```

---

## 다국어 지원

### 사용 가능 언어
- 🇰🇷 **한국어** (ko)
- 🇨🇳 **중국어** (zh)
- 🇺🇸 **영어** (en)

### 사용 방법

```typescript
import { useTranslation } from '@/lib/i18n/global-fulfillment';

function MyComponent() {
  const { t, lang } = useTranslation('zh'); // 중국어로 변경
  
  return (
    <div>
      <h1>{t('menu.dashboard')}</h1>
      <button>{t('button.upload')}</button>
    </div>
  );
}
```

### 번역 키 예시
```typescript
// 메뉴
'menu.dashboard' → '仪表板' (zh) / 'Dashboard' (en)
'menu.drop_shipping' → '代发货' (zh) / 'Drop Shipping' (en)

// 상태
'status.pending' → '待处理' (zh) / 'Pending' (en)
'status.completed' → '已完成' (zh) / 'Completed' (en)

// 배송 방법
'shipping.air' → '航空' (zh) / 'Air' (en)
'shipping.express' → '快递' (zh) / 'Express' (en)
```

---

## 설치 및 사용

### 1. 데이터베이스 설정

Supabase 또는 PostgreSQL에서 스키마를 실행합니다:

```bash
# 스키마 생성
psql < wms-app/supabase-global-fulfillment-schema.sql

# 샘플 데이터 삽입 (선택)
psql < wms-app/supabase-global-fulfillment-sample-data.sql
```

### 2. 환경 변수 설정

`.env.local` 파일에 Supabase 정보를 추가합니다:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
```

### 3. 의존성 설치 및 실행

```bash
cd wms-app
npm install
npm run dev
```

### 4. 브라우저에서 접속

```
http://localhost:3000/global-fulfillment
```

---

## 🎨 UI/UX 특징

### 컬러 코딩
각 프로세스 단계별로 고유한 색상을 사용하여 직관적인 식별이 가능합니다:

- 🟢 **드롭시핑**: 초록색 (입고)
- 🔵 **상품 준비**: 파란색 (검수)
- 🟣 **파도 관리**: 보라색 (포장)
- 🟠 **2차 정렬**: 주황색
- 🔴 **이상 처리**: 빨간색

### 실시간 업데이트
- 각 단계 완료 시 자동으로 다음 단계로 이동
- 이상 발생 시 즉시 알림
- 통관 상태 실시간 추적

### 모바일 대응
- 반응형 디자인
- 바코드 스캔 지원 (모바일 카메라)
- 터치 친화적 인터페이스

---

## 📝 향후 개선 사항

### Phase 2 (추후 구현)
- [ ] WeChat 알림 연동
- [ ] 실시간 추적 지도 (배송 위치)
- [ ] AI 기반 통관 지연 예측
- [ ] 자동 재고 할당 최적화
- [ ] 다중 창고 지원
- [ ] 고객 포털 (자가 조회)
- [ ] 모바일 앱 (React Native)

### Phase 3 (고급 기능)
- [ ] 블록체인 기반 이력 추적
- [ ] IoT 센서 연동 (온도, 습도)
- [ ] 머신러닝 수요 예측
- [ ] 로봇 자동화 연동

---

## 📞 지원

질문이나 문제가 있으시면 언제든지 연락주세요:
- Email: support@anhwms.com
- WeChat: anhwms_support

---

## 라이센스

MIT License

---

**Made with ❤️ for Global E-commerce**

